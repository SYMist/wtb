# AdSense 수익화 웹페이지 프로젝트

> ⚠️ **운영방침 우선**: 작업 전 `DIRECTION.md`를 먼저 읽는다 — **맨 위 갱신 배너부터.** (2026-05-30 **Tooly = 부업 메인 · "한국 금융 의사결정 깊이 포털"** → 2026-08 전황판이 유입 채널·승리 표면·회수 판정을 갱신했다. 아래 "목적"보다 이 방침이 상위이고, **`DIRECTION.md`와 볼트 전황판이 충돌하면 볼트가 이긴다.**)

## 목적

Google AdSense를 통해 수익화할 수 있는 웹페이지를 기획, 설계, 개발하는 프로젝트.
(※ 방향 갱신: AdSense 단일 의존 → 금융 제휴 중심. 상세는 `DIRECTION.md`.)

## Avatar 지식 허브 연동 — 작업 워크플로우

이 레포 = **실행 세션**(코드·배포·투두). 분석·지식은 **Avatar 볼트**가 보유한다.
Avatar 경로: `/Users/suyeon/Library/Mobile Documents/iCloud~md~obsidian/Documents/Avatar/`

- **역할 경계**: 데이터 분석·지식화 = Avatar / 코드·콘텐츠 발행·투두 실행 = 이 레포.
- **작업 시작 전 참조**: Avatar **`TASKS.md`(할 일 정본)** + `log.md` 최신 handoff(맥락 복원용) + 관련 wiki. ⚠️ **둘이 어긋나면 `TASKS.md`가 이긴다** — `log.md`는 append-only라 낡아도 못 고친다.
  - 지휘 교리·전황판(병목·다음 한 수·판정선): `COMMAND.md` §web — **여기가 방향의 정본**
  - 전략·방향: `wiki/web/overview.md`(5/30 재정의), `wiki/web/seo.md`
  - 성과·인사이트: `wiki/web/performance.md` (네이버 유입·GA 분석, 페이지 점검, 다음 액션)
- **데이터 분석은 Avatar에서**: 네이버 블로그 통계·GA4 export 원본은 Avatar `raw/web/`에 두고(애널리틱스는 `raw/web/naver-posts/tooly-stats/`) Avatar 세션이 분석→`wiki/web/performance.md`로 종합. 이 레포는 그 **결론을 참조해 실행**만 한다.
- **이 레포의 실행 투두는 이 레포에서 관리**(`TODO.md` — 정본. `WORKLOG.md`는 2026-05-30 이후 정지) — 볼트 밖(볼트 순수성, 2026-05-30). ⚠️ **2026-08-23 범위 명확화**: 「Avatar엔 투두를 두지 않는다」가 아니다. 볼트 밖에 두는 건 **작업세션 실행 투두**이고, **Avatar 자기 몫 목록은 볼트 `TASKS.md`**에 있다. 이 레포 투두를 `TASKS.md`에 베끼는 것도 금지(정본이 갈라진다).
- **작업 후 push**: 발행·배포 결과나 새 분석거리는 Avatar `raw/web/`에 요약을 남겨 ingest 트리거로 삼는다.
- (전략·방향 상위 = `DIRECTION.md` + 볼트 `COMMAND.md` §web 전황판. ⚠️ **「양도세 1줄기 집중」은 2026-08-05에 반박됐다** — 이긴 표면은 **데이터 시계열**(`/data/rates/base`·`/data/rates/mortgage`)이고 양도세 창끝은 CTR 1.1~1.5% 약패.)

### ⚠️ Avatar log.md 기록 — 타입 구분 (ingest vs handoff)

| 타입 | 언제 | 효과 |
|---|---|---|
| **ingest** | `raw/web/`에 배포·실측 요약 파일을 **추가**할 때(배포 환류 등) | wiki 지식화 대상 |
| **handoff** | 세션을 **마무리**하며 작업 상태를 넘길 때 | SessionStart 훅이 **자동 주입**해 다음 세션이 이어받음 |

둘 다 해당하면(배포 + 세션 종료) 둘 다 쓴다.

> 🔴 **handoff에 「이어서 할 일」 목록을 쓰지 않는다 (볼트 2026-08-23 폐지).** 할 일의 정본은 볼트 `TASKS.md`다 — handoff에 다시 적으면 그 순간 사본이 되고 `log.md`는 append-only라 낡아도 못 고친다. 넘길 액션은 **`TASKS.md`에 등록**하고, handoff엔 *「이번 세션이 `TASKS.md`를 어떻게 바꿨나」 한 줄*만.

### 📌 발주 리듬 — 월 최소 1건 ①신설 (볼트 §실행 리듬 3)

- **①신설 = 새 기능·새 페이지.** ②기존 심화·③수리는 이 카운트를 **대체 못 한다**(따로 센다). 블로그 글도 ①에 안 든다.
- 금지된 것은 *검증 없는 프로그래매틱 양산*이지 **신설이 아니다.**
- 신설분엔 전부 **판정선(틀림 조건·판정일)**을 단다. 판정에서 죽은 페이지는 정리(통합·noindex·삭제).
- **배포 완료 기준** — 라이브 배포만으론 완료가 아니다. 환류 검증에 **`git status -sb`로 `origin/main` 반영 확인**을 항상 포함한다(미반영이면 스케줄 봇이 되돌린다). 새 GA4 이벤트면 **배포 당일 라이브 발화 1건**까지가 완료.

## 팀 구성 및 역할

이 워크스페이스에서는 아래 역할의 전문가들이 회의를 통해 제품의 방향성, 성격, 기능을 결정한다. 모든 구성원은 각 분야에 해박한 지식과 탁월한 제품 감각을 갖추고 있으며, 회의 시 생산적인 의견과 비판적인 의견을 균형 있게 제시한다. 각 회의 종료 시에는 반드시 통일된 의견과 아이템을 도출한다.

| 역할 | 담당 영역 |
|---|---|
| **PO (Product Owner)** | 제품 전략, 비즈니스 가치, 우선순위 결정 |
| **PM (Product Manager)** | 실행 계획, 스펙 정의, 일정/리소스 관리 |
| **UX/UI 디자이너** | 사용자 경험, 인터페이스 설계 |
| **프로덕트 디자이너** | 제품 구조, 정보 설계, 기능 흐름 |
| **FE 엔지니어** | 프론트엔드 기술 및 구조 설계 |
| **BE 엔지니어** | 백엔드 기술 및 구조 설계 |
| **마케터** | 유저 접근 전략 및 실행 방안 수립 |

## 회의 운영 원칙

- 각 회의는 명확한 목적을 가지고 진행한다.
- 구성원은 목적에 맞는 생산적 의견과 비판적 의견을 모두 제시한다.
- 회의 종료 시 반드시 통일된 결론과 구체적 아이템을 도출한다.

## 회의록 작성 원칙

- 모든 미팅 회의록은 요약된 형태가 아닌 **실제 발언 형태**로 저장한다.
- 각 발언자의 역할과 발언 내용을 그대로 기록한다.
- 회의 말미에 결론 테이블과 다음 안건을 정리한다.
