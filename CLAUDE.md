# Tooly — 실행 프로젝트

Tooly는 한국 금융 의사결정용 계산기와 데이터 페이지를 운영한다. 현재 우선순위는 네이버 유입, 특히 `/data` 시계열 페이지다. 범용 계산기 확장이나 Google 노출만을 위한 작업은 우선하지 않는다.

## 시작 순서

1. `CURRENT.md` — 현재 작업·완료 기준·대기 조건
2. 변경 대상 코드 또는 관련 `research/` 메모
3. 필요한 Avatar 계약 또는 근거
4. 코드 변경일 때 `tooly/AGENTS.md`와 관련 Next.js 문서

`DIRECTION.md`, 과거 `TODO.md`, `WORKLOG.md`는 시작 입력이 아니다. `BACKLOG.md`는 사용자가 후보 검토를 요청할 때만 읽는다.

## 절대 원칙

- 문서는 짧고 단순하게 유지한다. 같은 규칙은 한 곳에만 두며, 정정은 기존 문장을 교체한다. 날짜별 시행착오는 공통 진입 문서에 남기지 않는다.
- Avatar는 사용자 지시 범위에서 판단·지시·검수하고, Tooly 작업자는 이 저장소의 구현·문서·자체 검증을 맡는다. Avatar 정본은 Avatar가 반영한다.
- 이미 승인된 범위는 다시 승인받지 않는다. 사용자 승인과 도구의 실제 실행 가능 상태는 구분하며, 명시적 차단은 우회·반복하지 않는다.

## Avatar와 실행 환경

### 로컬

Avatar가 기존 작업으로 지시를 보내고 `wait_threads` 완료 이벤트로 최종 응답을 회수한다. 작업자는 종료 시 자신의 최종 응답에 결과를 남기면 반환이 끝난다. 역방향 메시지·별도 완료 신호·`AVATAR_ROOT`·사용자 복사는 로컬 완료의 전제 조건이 아니다. 주기적 폴링과 새 자동화는 만들지 않는다.

### 클라우드

확인된 독립 클라우드 환경에서만 사용자 코드블록 수동 전달을 쓴다. 로컬 로그인·파일·MCP가 자동 공유된다고 가정하지 않는다. 상세 설정·검증 명령은 [클라우드 작업 안내](docs/cloud-work.md)에 둔다.

### 선택적 Avatar 원문

`AVATAR_ROOT`는 필요한 Avatar 계약·근거를 읽는 선택 경로다. 제공되면 `$AVATAR_ROOT/wiki/portfolio/templates/tooly.md`의 필요한 절만 읽고, 전체 템플릿을 복사하거나 시작 필독으로 만들지 않는다. 원문이 정말 필수인데 없으면 그 항목만 구체적으로 표시하되, 이미 수행한 결과는 반환한다.

- 키워드·SERP·신규 페이지: §1~3
- 기존 페이지 개선·구성: §4~5
- 계측: §6
- 블로그 원고·전환 판독: §7~8
- 색인 감사: §9
- 실행 지시·결과 반환: §10

## 조사와 기록

요청 범위 안에서 접근 가능한 자료를 수집한다. 첫 경로가 실패하면 허용된 공식 API/export·상세 보고서·공식 검색·브라우저 UI 중 실행 가능한 대안을 수행한다. 반복 실패·접근 통제 우회는 하지 않는다. 로그인·홈·스니펫은 상세 원행·동일 기간·공식 원문의 대체 증거가 아니다.

결측은 `확보`, `부분 확보`, `미시도·진행 가능`, `현재 환경에서 차단`, `사용자 개입 필요`, `원천 미제공·처리 대기`로 구분한다. 남은 결측만 실제 시도·오류·대안·필수 사용자 조치 또는 재개 조건과 함께 보고한다. 비밀번호·토큰은 요구하지 않는다.

- 현재·다음·조건부 작업: `CURRENT.md`
- 보류 후보: `BACKLOG.md`
- 채택 전 조사: `research/YYYY-MM-DD-주제.md`
- 코드와 배포 결과: 코드·Git 이력·필요한 작업 문서
- 전략을 바꿀 사실·증거·위험: Avatar에 반환

## 검증과 외부 실행

금융 계산은 대표 입력값과 관련 테스트, 계측은 실제 이벤트 도달, 국소 카피·링크·문서는 변경 범위만 확인한다. 배포와 네이버 블로그 발행은 사용자 승인 후에만 진행한다. 종료 시 바뀐 사실과 다음 결정에 필요한 위험만 짧게 남긴다.
