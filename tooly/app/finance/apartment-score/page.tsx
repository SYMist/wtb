import ApartmentScoreClient from "./ApartmentScoreClient";

// 인스타→방법론 자료→계산기 퍼널의 종착(인터랙티브)면.
// 백엔드/DB 없이 로컬스토리지만 사용 — 정적 프리렌더.
export default function ApartmentScorePage() {
  return <ApartmentScoreClient />;
}
